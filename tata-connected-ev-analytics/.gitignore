@"
# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
venv/
.env

# Data (raw/clean/processed are regenerated by src/data_generation and src/etl —
# don't bloat the repo with large CSVs; keep .gitkeep placeholders instead)


# Power BI
powerbi/*.pbix.bak

# OS
.DS_Store
"@ | Out-File -FilePath .gitignore -Encoding utf8